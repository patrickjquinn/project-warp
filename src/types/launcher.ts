export interface RecentProject {
    name: string;
    path: string;
    last_opened: string;
    template: string;
}

export interface Template {
    name: string;
    description: string;
    icon: string;
    template: string;
}
